db = db.getSiblingDB('security')

print("START 318_security_ref.js");

db.contexts.updateOne( {
	"_id": "ui_referential_context"
}, {
    $addToSet: {
        "roleNames":  {
            $each: [
            	"ROLE_GET_ACCESSION_REGISTER"
        	]
        }
    }
});

print("END 318_security_ref.js");
